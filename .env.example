# Django
DJANGO_SECRET_KEY=votre-cle-secrete-tres-longue-et-securisee
DJANGO_DEBUG=True

# Base de données (SQLite par défaut)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# PostgreSQL (optionnel - décommentez pour la production)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=ajcm_db
# DB_USER=ajcm_user
# DB_PASSWORD=password_secure
# DB_HOST=localhost
# DB_PORT=5432

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Email (SMTP - pour production)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=votre-email@gmail.com
EMAIL_HOST_PASSWORD=votre-mot-de-passe-app
DEFAULT_FROM_EMAIL=AJCM <noreply@ajcm.ma>

# Redis (pour Celery - optionnel)
REDIS_URL=redis://localhost:6379